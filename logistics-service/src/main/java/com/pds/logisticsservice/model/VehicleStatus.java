package com.pds.logisticsservice.model;

//Enum that defines the states of the vehicle

//USED in VehicleSelectionService to filter which vehicles are eligible
public enum VehicleStatus {
    AVAILABLE,
    EN_ROUTE,
    MAINTENANCE
}
